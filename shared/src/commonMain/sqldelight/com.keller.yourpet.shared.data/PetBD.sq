CREATE TABLE PetBD (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    imageUrl TEXT NOT NULL
);

selectAllPets:
SELECT * FROM PetBD;

insertPet:
INSERT INTO PetBD(id, name, imageUrl)
VALUES(?, ?, ?);

deletePet:
DELETE FROM PetBD WHERE id = ?;
